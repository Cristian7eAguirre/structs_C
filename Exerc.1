#include <stdio.h>
#include <stdbool.h> //Para o uso do bool (lógico)

typedef struct {
    char nome[50];
    float salario;
    int idade;
    bool sexo;
} REG;

int main() {
    REG funcionario;

    return 0;
}
